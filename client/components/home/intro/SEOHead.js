import { useEffect } from 'react';
import { siteConfig, structuredData, breadcrumbData, faqData } from './Data';

// This component is no longer needed as we're using Next.js generateMetadata
// Keeping it for backward compatibility but it's now a no-op
const SEOHead = () => {
  return null;
};

export default SEOHead;